import React from 'react'


// I created this file on root folder, this means before rendering all things, it will render first
const Loading = () => {
  return (
    <div>___________________</div>
  )
}

export default Loading