'use client'
import React from 'react'
// In nay page something happens then this error.tsx will render
// Example: If you have error.tsx at home directory then it will shown, If you need specific error through like in (buyer) Route group then you can show my making an error file their then the global error won't display the nearest will display the error.tsx file
const error = () => {
  return (
    <div>error.... OUUUUHHH</div>
  )
}

export default error